---

- name: Install collectd packages
apt: name=collectd state=present

- name: create collectd data folders
  file:
    path: "{{ item }}"
    state: directory
    mode: 0775
  with_items:
      - "{{ collectd_data_path }}"
      
